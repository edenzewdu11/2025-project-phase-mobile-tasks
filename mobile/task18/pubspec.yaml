name: contracts_of_data_sources
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'  # More flexible version constraint

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  uuid: ^4.2.2
  connectivity_plus: ^5.0.2  # More reliable connectivity checking
  shared_preferences: ^2.2.2
  http: ^1.1.0

  # ADDED for BLoC implementation
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  get_it: ^7.6.4  # More stable version

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.3
  mocktail: ^1.0.0

  # ADDED for BLoC testing
  bloc_test: ^9.1.5  # Version compatible with flutter_bloc 8.x

flutter:
  uses-material-design: true